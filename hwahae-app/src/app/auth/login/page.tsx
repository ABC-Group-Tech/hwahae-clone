import LoginPageClient from "@/components/auth/login-page-client"

export const metadata = {
  title: "로그인 | ABCPharm",
  description: "ABCPharm 로그인 페이지",
}

export default function LoginPage() {
  return <LoginPageClient />
}
